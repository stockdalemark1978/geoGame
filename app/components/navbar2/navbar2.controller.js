class navbar2Controller {
    constructor($rootScope, $interval) {
        let ctrl = this;
        ctrl.$rootScope = $rootScope;
        ctrl.title = "MarkMail";
    };

}

export default navbar2Controller;